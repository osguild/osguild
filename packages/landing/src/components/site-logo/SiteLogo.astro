---
import { getLocaleFromUrl } from "@js/localeUtils";
import { getTranslatedData } from "@js/translationUtils";


const currLocale = getLocaleFromUrl(Astro.url);
const _siteData = getTranslatedData("siteData", currLocale);

---

<a
	class="primary-focus text-foreground flex h-10 items-center align-middle text-2xl font-bold"
	href={getLocalizedRoute(currLocale)}
	class=""
>
	<!-- <Image
		src={logo}
		alt="Cosmic Themes logo"
		loading="eager"
		width={100}
		height={100}
		class="h-14 w-14"
		aria-hidden="true"
	/> -->
	<div class="">{siteData.name}</div>
	<span class="text-primary">.</span>
</a>
